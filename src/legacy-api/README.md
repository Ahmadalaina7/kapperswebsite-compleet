# API route sources kept for a future Node/Vercel deploy.
# They are outside `src/app` because this project uses `output: 'export'`
# (Plesk static hosting) and Next.js cannot export App Router API routes.
#
# To restore on Node hosting:
#   move these folders back to `src/app/api/`
#   remove `output: 'export'` from next.config.ts
